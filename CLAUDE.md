# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# ═══════════════════════════════════════════
# 固定执行准则（永久有效，全程不可跳过）
# ═══════════════════════════════════════════

1. 所有回复正文开头固定前缀：老板，

2. 接收用户任意提问/需求后，禁止立刻落地执行、禁止直接输出成品内容；
   第一步：精简复述你的理解，分点拆解用户原始诉求，标注用户隐含需求、遗漏条件、边界盲区、不合理设定；
   第二步：基于用户原始方案，针对性列出 >= 2 套优化方案，分别写明：优化方向、改进优势、适用场景、潜在改动点；
   第三步：主动向用户确认：「选用原方案执行 / 选用优化方案A / 选用优化方案B / 自定义调整需求」；
   只有收到用户明确选择指令后，才按照选定方案正式输出内容；无确认指令，持续停留在方案确认环节。

3. 补充约束：若用户需求信息缺失（参数、用途、环境、使用目标模糊），优先补充提问补齐关键信息，再生成优化方案；禁止脑补缺省条件直接动工。
4. 每次对话后，自动更新 当日修改记录文件
5. **禁止私自提交（强制执行，不可跳过）**：任何情况下，禁止未经用户明确同意执行 `git commit`、`git push`、`git merge`、`git rebase` 等影响版本控制的命令。只有当用户明确说「提交」、「commit」、「push」时，才能执行相应操作。违反此规则将导致严重后果。

# ═══════════════════════════════════════════
# 操作指令（按触发条件执行）
# ═══════════════════════════════════════════

**创建今日修改记录**：当用户发送「创建今日修改记录」指令时，执行以下步骤：
1. 读取 `docs/修改记录/` 目录下对应月份下所有文件名，提取最大章节号 N
2. 读取 `docs/修改记录/模板.md` 模板内容
3. 将模板中所有日期占位符（如 `2026-06-05`）替换为当天日期（格式 `YYYY-MM-DD`）
4. 创建新文件 `docs/修改记录/第N+1章 修改记录M-DD.md`（M-DD 为当天月-日） 到到当前月份的文件夹下
5. 文件内容为替换日期后的模板内容

**读取错误截图后清理**：当读取 `docs/参考/` 目录下的图片文件后，立即删除该文件，避免图片文件积压。

**回复重点记录（自动执行，不可跳过）**：每次完成用户需求后（代码修改、功能实现、问题修复等），必须**立即**将回复重点记录到当天的修改记录文件中，**不需要等待用户命令**：
1. 定位当天的修改记录文件：`docs/修改记录/{M}月/第N章 修改记录{M}-{D}.md`
2. 在对应模块的标题下（如「宠物模块」），追加本次修改的简要说明
3. 记录格式：`- [功能名称]：简要描述修改内容和实现方式`
4. 若当天修改记录文件不存在，先执行「创建今日修改记录」指令
5. **执行时机**：每次代码修改、功能实现、问题修复完成后，立即更新，不要等用户说"更新修改记录"
6. 此操作为强制执行，不可跳过

**修改记录时间戳规范**：在修改记录文件中，需要为每个用户提问和回复添加时间戳：
1. 用户提问位置：在「**提问：**」后添加时间戳 `[HH:MM]`
2. 回复结果位置：在「**修复：功能名称**」后添加时间戳 `[HH:MM]`
3. 时间格式：24小时制，如 `[08:30]`、`[14:15]`
4. 时间获取方式：使用 `date '+%H:%M'` 命令获取当前时间，不要猜测
5. 匹配优化：由于修改记录文件内容较多，使用用户提问的**前20个字符**作为搜索关键词定位位置，避免全文扫描
6. 示例格式：
   ```
   **提问：** `[08:30]`
   1. 用户的具体问题内容...

   **修复：功能名称** `[08:45]`
   - 问题描述...
   - 修复方案...
   ```

**docs 文件元数据规范（强制执行，不可跳过）**：在 `docs/` 目录下新建文件时（`docs/修改记录/` 目录除外），必须在文件顶部添加 YAML frontmatter 元数据块：
1. **适用范围**：`docs/` 下所有 `.md`、`.txt`、`.log`、`.json` 等文本文件
2. **排除范围**：`docs/修改记录/` 目录下的所有文件、图片文件（`.png`、`.jpg`、`.jpeg`、`.gif` 等）
3. **必须字段**：
   ```yaml
   ---
   description: 文件简要描述（1句话，从文件名或内容首行提取）
   created: YYYY-MM-DD（文件创建日期，使用文件系统时间）
   last_modified: YYYY-MM-DD（最后修改日期，使用文件系统时间）
   ---
   ```
4. **格式要求**：
   - frontmatter 必须是文件最顶部的内容，前面不能有空行
   - `description` 字段：从文件名或内容首行提取，长度 5-80 字符
   - 时间字段：使用 `stat -c %w`（创建时间）和 `stat -c %y`（修改时间）获取，格式 `YYYY-MM-DD`
5. **执行时机**：每次创建新文件后，立即添加 frontmatter，不要等用户提醒
6. **已有文件处理**：如果文件已有 frontmatter，保持不变；如果没有，需要补添加

---

# ═══════════════════════════════════════════
# 微信小游戏开发架构规范（强制执行，不可跳过）
# ═══════════════════════════════════════════

> 本规范适用于 wasteland-tamer-mini-game 微信小游戏项目，所有代码修改必须遵守。

## 1. 项目架构规范

### 1.1 目录结构标准
```
├── game.js                          // 游戏入口（禁止在此编写业务逻辑）
├── game.json                        // 游戏配置（禁止手动修改运行时配置）
├── js/
│   ├── main.js                      // 游戏主循环（只负责 update/render 调度）
│   ├── render.js                    // Canvas 初始化
│   ├── databus.js                   // 全局状态管理（单例）
│   ├── base/                        // 基础类（精灵、动画、对象池）
│   ├── player/                      // 玩家相关模块
│   ├── npc/                         // NPC/敌人模块
│   ├── runtime/                     // 运行时模块（背景、UI、音效）
│   ├── systems/                     // 系统模块（战斗、宠物、地图等）
│   ├── managers/                    // 管理器（资源、事件、存档等）
│   ├── config/                      // 配置文件（数值表、常量）
│   ├── utils/                       // 工具函数
│   └── libs/                        // 第三方库（禁止修改）
├── images/                          // 图片资源（按模块分子目录）
├── audio/                           // 音频资源
└── docs/                            // 项目文档
```

### 1.2 文件职责单一原则
- **每个文件只做一件事**，禁止在一个文件中堆砌多个不相关的功能
- **单文件行数上限**：
  - 基础类（base/）：≤ 300 行
  - 游戏模块（player/、npc/、runtime/）：≤ 400 行
  - 系统模块（systems/）：≤ 500 行
  - 管理器（managers/）：≤ 300 行
  - 配置文件（config/）：≤ 200 行
  - 工具函数（utils/）：≤ 150 行
- 超过行数限制时，必须拆分为多个文件，按子功能组织

### 1.3 模块化要求
- 所有模块使用 ES6 Module（`import/export`），禁止使用 CommonJS（`require/module.exports`）
- 每个模块文件必须有文件头注释，说明模块职责
- 模块间通过公共接口通信，禁止跨模块直接访问内部实现（如 `enemy._hp`）
- 循环依赖：禁止出现模块循环引用，如发现必须重构

## 2. 命名规范（强制执行）

### 2.1 文件命名
| 类型 | 命名规则 | 示例 |
|------|---------|------|
| 类文件 | camelCase（首字母小写） | `enemy.js`、`background.js`、`gameinfo.js` |
| 配置文件 | camelCase | `gameConfig.js`、`petData.js` |
| 工具文件 | camelCase | `mathUtil.js`、`eventBus.js` |
| 入口文件 | 固定命名 | `main.js`、`render.js` |
| 索引文件 | 固定命名 | `index.js` |

### 2.2 变量/函数命名
| 类型 | 命名规则 | 示例 |
|------|---------|------|
| 普通变量 | camelCase | `enemyCount`、`bulletSpeed` |
| 常量 | UPPER_SNAKE_CASE | `MAX_ENEMY_COUNT`、`FRAME_RATE` |
| 类名 | PascalCase | `Player`、`Enemy`、`DataBus` |
| 方法名 | camelCase | `init()`、`update()`、`render()` |
| 私有属性 | 下划线前缀 | `_hp`、`_speed`、`_instance` |
| 布尔变量 | is/has/can 前缀 | `isGameOver`、`hasBuff`、`canAttack` |

### 2.3 禁止的命名
- 禁止使用单字母变量（循环变量 `i`/`j`/`k` 除外）
- 禁止使用中文拼音命名
- 禁止使用保留字和关键字作为变量名
- 禁止使用 `temp`、`data`、`obj` 等模糊命名

## 3. 代码风格规范

### 3.1 基础格式
- **缩进**：2 空格，禁止使用 Tab
- **分号**：每行语句必须以分号结尾
- **引号**：字符串统一使用单引号 `'`，模板字符串除外
- **花括号**：`if`/`for`/`while` 等必须使用花括号，即使只有一行
- **空行**：函数之间空一行，逻辑块之间空一行，文件末尾保留一个空行

### 3.2 注释规范
```javascript
/**
 * 类/函数的 JSDoc 注释（必须包含功能说明）
 * @param {类型} 参数名 - 参数说明
 * @returns {类型} 返回值说明
 */

// 行内注释：说明"为什么"，而不是"做什么"
// 错误示例：i++;  // 自增
// 正确示例：i++;  // 跳过已销毁的敌人

// 块注释：用于大段逻辑分隔
// ---- 敌人生成逻辑 ----
```

### 3.3 禁止的代码写法
```javascript
// ❌ 禁止：eval 动态执行
eval('console.log("bad")')

// ❌ 禁止：with 语句
with (obj) { ... }

// ❌ 禁止：arguments.callee
arguments.callee()

// ❌ 禁止：八进制字面量
var num = 010;

// ❌ 禁止：比较时使用 == 或 !=（必须用 === 或 !==）
if (a == b) { ... }

// ❌ 禁止：逗号运算符
var a = (1, 2);

// ❌ 禁止：void 运算符
void 0;
```

## 4. 微信小游戏性能规范（强制执行）

### 4.1 内存管理
- **对象池（必须使用）**：所有频繁创建/销毁的对象（子弹、敌人、特效）必须使用对象池 `Pool` 类
- **禁止 new 频繁创建对象**：游戏循环中禁止使用 `new`、`Array()`、`{}` 创建临时对象
- **及时回收**：对象不再使用时，必须调用对象池回收方法，禁止依赖 GC
- **弱引用**：事件监听器、定时器必须在对象销毁时移除，防止内存泄漏

### 4.2 Canvas 渲染优化
- **离屏 Canvas**：静态内容（背景、UI）使用离屏 Canvas 预渲染，避免每帧重绘
- **脏矩形**：只重绘变化区域，禁止每帧清空整个画布重绘所有元素
- **图片缓存**：图片加载后缓存到全局变量，禁止每帧重复创建 Image 对象
- **帧率控制**：目标帧率 60fps，单帧渲染时间不超过 16ms
- **drawImage 优化**：避免在 drawImage 中使用裁剪参数（source rect），尽量使用预处理后的图片

### 4.3 资源加载策略
- **预加载**：关键资源（角色、背景、UI）在游戏启动时预加载
- **按需加载**：非关键资源（音效、过场动画）在使用时延迟加载
- **资源复用**：相同资源禁止重复加载，通过资源管理器统一管理
- **包体控制**：单个图片 ≤ 200KB，总包体 ≤ 4MB（微信限制）
- **格式要求**：图片使用 PNG（透明）或 JPG（非透明），音频使用 MP3

### 4.4 触摸事件处理
- **事件委托**：使用事件冒泡机制，禁止为每个游戏元素单独绑定触摸事件
- **节流处理**：高频触摸事件（touchmove）必须节流，防止卡顿
- **坐标转换**：触摸坐标必须转换为画布坐标，考虑设备像素比和画布缩放

## 5. 状态管理规范

### 5.1 DataBus 单例模式
- 全局状态统一通过 `GameGlobal.databus` 访问
- 禁止在模块内部维护独立的游戏状态副本
- 状态变更必须通过 DataBus 的方法，禁止直接修改属性

### 5.2 状态分类
```javascript
// 游戏状态（通过 DataBus 管理）
frame          // 当前帧数
score          // 分数
isGameOver     // 游戏结束标志
enemys         // 敌人列表
bullets        // 子弹列表

// 模块状态（通过模块自身管理）
player.hp      // 玩家血量
enemy.speed    // 敌人速度
```

### 5.3 事件通信
- 模块间通信使用事件总线（EventEmitter）
- 禁止模块间直接调用对方方法（解耦）
- 事件名使用 PascalCase：`'GameOver'`、`'ScoreChanged'`

## 6. 错误处理规范（强制执行）

### 6.1 异常捕获
```javascript
// ✅ 正确：所有风险代码必须 try/catch
try {
  const data = JSON.parse(jsonString);
} catch (error) {
  console.error('[数据解析] JSON解析失败:', error.message, jsonString);
}

// ❌ 错误：空 catch 块
try {
  riskyOperation();
} catch (e) {
  // 空 catch 块禁止
}
```

### 6.2 日志规范
```javascript
// console.error：业务报错（必须包含中文场景描述）
console.error('[敌人生成] 创建敌人对象失败:', error);

// console.warn：告警降级（非致命问题）
console.warn('[资源加载] 图片加载超时，使用默认图片');

// console.log：禁止提交到线上代码（调试日志必须删除）
// ❌ console.log('debug info');  // 禁止
```

### 6.3 边界检查
- 数组访问前必须检查索引范围
- 除法运算前必须检查除数
- 对象属性访问前必须检查是否存在
- 触摸事件坐标必须在画布范围内

## 7. 资源管理规范

### 7.1 图片资源
```
images/
├── ui/              // UI 元素
├── player/          // 玩家相关
├── enemy/           // 敌人相关
├── effects/         // 特效
├── maps/            // 地图
└── icons/           // 图标
```
- 文件命名：`模块_描述_状态.png`，如 `player_idle_01.png`
- 禁止使用中文文件名
- 禁止使用空格和特殊字符

### 7.2 音频资源
```
audio/
├── bgm/             // 背景音乐
├── sfx/             // 音效
└── voice/           // 语音
```
- 文件命名：`类型_描述.mp3`，如 `sfx_attack.mp3`
- BGM 建议使用循环播放，音效使用单次播放

## 8. Git 版本控制规范

### 8.1 分支管理
- `master`：稳定版本，只接受 PR 合并
- `develop`：开发主分支
- `feature/xxx`：功能开发分支
- `fix/xxx`：Bug 修复分支

### 8.2 提交规范
```
<type>(<scope>): <subject>

type:
  feat     - 新功能
  fix      - 修复 Bug
  style    - 代码格式（不影响功能）
  refactor - 重构（既不修复 Bug 也不增加功能）
  perf     - 性能优化
  test     - 增加测试
  docs     - 文档变更
  chore    - 构建/辅助工具变动

scope: 模块名（player/enemy/system/ui 等）
subject: 简要描述（中文，不超过 50 字）

示例：
feat(player): 实现玩家宠物召唤功能
fix(enemy): 修复敌人碰撞检测越界问题
perf(render): 优化Canvas重绘性能
```

## 9. 测试规范

### 9.1 单元测试
- 核心逻辑（伤害计算、碰撞检测、数值配置）必须编写单元测试
- 测试文件放置在 `js/__tests__/` 目录
- 测试文件命名：`模块名.test.js`

### 9.2 手动测试清单
- 功能开发完成后，必须检查：
  - 真机运行流畅度（帧率 ≥ 55fps）
  - 内存无明显增长（连续运行 5 分钟）
  - 触摸响应正常
  - 音频播放正常
  - 不同屏幕尺寸适配

## 10. 代码自检清单（每次修改后必须执行）

每次代码修改完成后，必须逐项检查：

- [ ] 文件行数未超过限制
- [ ] 文件职责单一，未堆砌无关功能
- [ ] 命名符合规范（camelCase/PascalCase/UPPER_SNAKE_CASE）
- [ ] 所有风险代码有 try/catch 包裹
- [ ] catch 块内有中文日志输出
- [ ] 无 console.log 残留（调试日志已删除）
- [ ] 频繁创建/销毁的对象使用了对象池
- [ ] 事件监听器在对象销毁时移除
- [ ] 模块间通过事件通信，未直接调用内部方法
- [ ] 代码缩进、分号、引号符合格式要求
- [ ] 无循环依赖
- [ ] 新增文件已添加 docs 元数据（如适用）
